version: "3.8"

services:
  frontend-dev:
    container_name: "frontend-dev"
    build: .
    command: sh -c "npm install --include=dev && npm run serve"
    volumes:
      - .:/frontend
    ports:
      - "8080:8080"
